<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ffffff">

    <com.google.android.gms.maps.MapView
        android:id="@+id/map"

        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".ui.main.map.UserLocationFragment"
        android:layout_above="@+id/lbl_location_hint"/>

    <!--android:name="com.google.android.gms.maps.SupportMapFragment"-->
    <!--class="com.google.android.gms.maps.SupportMapFragment"-->

    <TextView
        android:id="@+id/lbl_location_hint"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btn_submit_location"
android:textColor="#000000"
        android:gravity="center"
        android:padding="16dp"
        android:text="آدرس دقیق خود را بروی نقشه مشخص کنید"
        android:textSize="16dp" />

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/btn_submit_location"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:text="تایید"
        android:textSize="14dp"
        android:textColor="#ffffff"
        />


</RelativeLayout>